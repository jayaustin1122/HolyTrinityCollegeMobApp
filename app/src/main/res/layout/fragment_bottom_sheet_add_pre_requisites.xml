<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="24dp"
    android:paddingBottom="40dp"
    android:background="@drawable/background_bottom_sheet"
    android:orientation="vertical"
    tools:context=".users.admin.prerequisites.BottomSheetAddPreRequisitesFragment">

    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:id="@+id/handle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Subject Pre-Requisites"
        android:textSize="22sp"
        android:textColor="@color/dark_text_color"
        android:fontFamily="@font/roboto_medium" />

    <!-- Curriculum -->
    <TextView
        android:id="@+id/curriculumTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Curriculum *"
        android:textColor="@color/dark_icon_color"
        android:textSize="16sp" />

    <Spinner
        android:id="@+id/curriculumSpinner"
        android:layout_width="match_parent"
        android:layout_marginTop="5dp"
        android:layout_height="56dp"
        android:paddingVertical="10dp"
        android:paddingHorizontal="10dp"
        android:background="@drawable/background_spinner"
        android:spinnerMode="dropdown" />

    <!-- Subject -->
    <TextView
        android:id="@+id/subjectTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Subject *"
        android:textColor="@color/dark_icon_color"
        android:textSize="16sp" />

    <Spinner
        android:id="@+id/subjectSpinner"
        android:layout_width="match_parent"
        android:layout_marginTop="5dp"
        android:layout_height="56dp"
        android:paddingVertical="10dp"
        android:paddingHorizontal="10dp"
        android:background="@drawable/background_spinner"
        android:spinnerMode="dropdown" />

    <!-- Requires -->
    <TextView
        android:id="@+id/requiresTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Requires *"
        android:textColor="@color/dark_icon_color"
        android:textSize="16sp" />

    <Spinner
        android:id="@+id/requiresSpinner"
        android:layout_width="match_parent"
        android:layout_marginTop="5dp"
        android:layout_height="56dp"
        android:paddingVertical="10dp"
        android:paddingHorizontal="10dp"
        android:background="@drawable/background_spinner"
        android:spinnerMode="dropdown" />


    <com.google.android.material.button.MaterialButton
        android:id="@+id/doneButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Done"
        android:textColor="@color/light_text_color"
        app:backgroundTint="@color/violet_primary" />

</LinearLayout>